# Customer Churn Prediction using KNN

This project implements a K-Nearest Neighbors (KNN) model to predict customer churn.
The model is deployed as a Streamlit web application.

## 📌 Features
- KNN classification model
- Preprocessing with ColumnTransformer
- One-hot encoding for categorical features
- Standard scaling for numerical features
- Interactive Streamlit interface
- Business-oriented churn recommendations

## 📂 Project Structure
